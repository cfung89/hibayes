models:
  - name: Binomial
    config:
      tag: version_1
  - name: Binomial
    config:
      tag: version_2
      random_effects:
        - name: task_effects # modify the default task_effect
          prior:
            distribution: normal
            distribution_args:
              loc: 0
              scale: 10
