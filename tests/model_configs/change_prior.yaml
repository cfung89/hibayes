models:
  name: Binomial
  config:
    tag: version_1
    configurable_parameters:
      name: overall_mean
      prior:
        distribution: normal
        distribution_args:
          loc: 0
          scale: 10
